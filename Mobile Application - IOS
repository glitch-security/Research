Jailbreak

Use old iphone OS - Pa1lrain exploit

1. Make sure to use USB A to Lightning cable - not type c to lightning

2. Use correct USB Port on PC - if not working with one port - try another.

3. When exploit complete, run exploit again with Create FAKEFS option to create fakefs on iphone.

4. Root password default - alpine

5. Root user default - mobile (not root!)

6. Start frida server in iphone for remote connections using the following command after ssh -  ./frida-server -l 0.0.0.0 & (to start it on all interface)

7. connecting frida to remote device - frida-ps.exe -H 192.168.1.17 -a

8. connecting objection to remote device -  objection.exe -N -h 192.168.1.17 -p 27042 -g com.appname explore

